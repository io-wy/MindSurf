output_dir: models/checkpoints/minimind_official_v1_80m
max_steps: 10000
batch_size: 32
accumulation_steps: 1
learning_rate: 5.0e-4
weight_decay: 0.01
max_grad_norm: 1.0
warmup_steps: 200
stable_ratio: 0.8
min_lr_ratio: 0.1
eval_every: 1000
eval_batches: 250
save_every: 500
checkpoint_keep_last: 2
logging_every: 10
device: auto
dtype: bfloat16
compile_model: false
